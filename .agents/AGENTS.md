# Behavior Guidelines

## Git Commits

Do not automatically create Git commits after completing a task or a step. Only commit changes when explicitly requested by the user. If a task is complete, notify the user and wait for their explicit permission before running any git commit commands.
