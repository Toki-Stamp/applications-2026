# 📋 Бэклог задач (Backlog)

Учет задач по улучшению, оптимизации, рефакторингу и новым фичам проекта **Google Antigravity Applications-2026**.

<details>
<summary><b>⚡ P0 — Критические оптимизации (High Priority / Performance)</b></summary>

- [x] **[P0-1] Оптимизация шрифта Material Symbols**
  `material-symbols-outlined.woff2` весил 3.96 МБ в `dist/`. Переведён на изолированные SVG-векторы через компонент `<md-icon>`. Эффект: сокращение бандла на ~3.96 МБ (~99.9%).

</details>

<details>
<summary><b>🛡️ P1 — Безопасность и инфраструктура (Security & Infrastructure)</b></summary>

- [x] **[P1-1] Блокировка транзакций в GAS (`LockService`)**
  Исключить гонку данных при одновременной отправке заявок при смене статуса `Obsolete` и записи в Google Sheets через `LockService.getScriptLock().waitLock(30000)`.
- [x] **[P1-2] Разделение тестов Vitest (Unit) и Playwright (E2E)**
  Изолировать юнит-тесты (`src/**/*.test.js` для `schema.js`, `utils.js`) от E2E-тестов (`tests/`) в конфигах `vitest.config.js` и `playwright.config.js`.
- [x] **[P1-3] Исправление static type-checking (`npx svelte-check`)**
  Устранить ошибки совместимости типов при выполнении `npx svelte-check` (обновить зависимости или скорректировать `tsconfig.json` / `jsconfig.json`).

</details>

<details>
<summary><b>🏗️ P2 — Архитектура и качество кода (Refactoring & Clean Code)</b></summary>

- [ ] **[P2-1] Декомпозиция компонента `ApplicationsGrid.svelte` (1 352 строки)**
  Разбить на `GridHeader.svelte` (шапка, пиннинг, фильтры), `GridRow.svelte` (строка участника) и `gridStore.svelte.js` (состояние фильтров и выделения).
- [ ] **[P2-2] Оптимизация реактивной валидации в `App.svelte`**
  Заменить `JSON.stringify(formStore.data)` и `setTimeout` в `$effect` на производные состояния Svelte 5 (`$derived`) или вызов валидации по событиям ввода.

</details>

<details>
<summary><b>💰 F1 — Финансовый модуль и Чек участника (Financial & Admin Features)</b></summary>

- [ ] **[F1-1] Админ-панель сметы (`admin.html`)**
  Страница с входом по ПИН-коду: ввод расходов по 3 категориям (Продукты, Алкоголь, Орг. расходы), настройка тарифов (маршрутка, проживание/ночь), переключение статуса сметы (`Предварительный` / `Финализирован`), отметка оплаты участников.
- [ ] **[F1-2] Портал «Чек участника» (`personal.html`)**
  Персональные начисления с поиском по имени/нику/телефону: расчёт долей (Питание, Алкоголь, Маршрутка, Проживание), скрытие орг. расходов («размазывание» в доли), отображение статуса оплаты (`🔴 Ожидает` / `🟢 Оплачено`).
- [ ] **[F1-3] Интеграция Google Apps Script (`doGet`)**
  Модернизация `doGet()` в `google_apps_script.js` для выгрузки из Таблицы колонок питания, алкоголя, ночевок и телефона.

</details>

<details>
<summary><b>♿ P3 — UX, Адаптивность и Доступность (UX & Accessibility)</b></summary>

- [ ] **[P3-1] Доступность с клавиатуры (a11y) в таблице**
  Добавить атрибуты accessibility (`tabindex="0"`) и обработчики `Enter`/`Space` для фильтров и строк таблицы.
- [ ] **[P3-2] Компактный режим таблицы для мобильных (320–375px)**
  Реализовать сворачивание пин-колонок или режим просмотра в виде мобильных карточек.
- [ ] **[P3-3] Взаимозачёты для закупщиков**
  Учёт личных расходов закупщиков, вычисление сальдо (возврат / доплата) и автоматическое вычитание из их итогового чека.
- [ ] **[P3-4] Расширение матрицы таблицы**
  Возможность переключения сетки таблицы не только для Транспорта, но и для Питания, Проживания или Сводного вида.

</details>
